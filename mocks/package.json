{
  "name": "eep-mock-api-server",
  "version": "1.0.0",
  "description": "EEP Mock api server serves response from static json files",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "json-server": "^0.16.3"
  }
}
